<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-primary">
    <div class="container-fluid">
      <img class="img-logo" src="../images/logo-pink.png">
      <a class="navbar-brand" href="/">
        <h1 class="text-danger">Tiempo</h1>
      </a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
        aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto">
          <li class="nav-item <%= path === '/' ? 'active' : ''%>">
            <a class="nav-link" href="/"><span class="sr-only"></span> Home</a>
          </li>
          <% if (isAuthenticated) { %>
          <li class="nav-item">
            <a class="nav-link" href="/time/card" aria-expanded="false"> Enter Time</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/time/total" aria-expanded="false"> Total Time</a>
          </li>
          <% } %>
          <% if (user!=undefined) { 
            if(user.role=="admin") {%>
          <li class="nav-item">
            <a class="nav-link" href="/time/totalbyemployee" aria-expanded="false"> Total Time by Employee</a>
          </li>
        <% } }%>
          <li class="nav-item">
            <a class="nav-link" href="/time/about">About</a>
          </li>
        </ul>
        <% if (isAuthenticated) { %>
        <ul class="navbar-nav ml-auto">
          <li class="nav-item <%= path === '/auth/logout' ? 'active' : ''%>"><a class="nav-link"
              href="/auth/logout">Logout</a></li>
        </ul>
        <% } else { %>
        <ul class="navbar-nav ml-auto">
          <li class="nav-item <%= path === '/auth/login' ? 'active' : ''%>"><a class="nav-link"
              href="/auth/login">Login</a></li>
          <li class="nav-item <%= path === '/auth/signup' ? 'active' : ''%>"><a class="nav-link"
              href="/auth/signup">Sign Up</a></li>
        </ul>
        <% } %>
      </div>
    </div>
  </nav>